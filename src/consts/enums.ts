enum DeviceType {
  CAR = 'CAR',
  MOBILE = 'MOBILE',
  LAPTOP = 'LAPTOP',
  PC = 'PC',
}

enum Status {
  SUCCESS = 'SUCCESS',
  ERROR = 'ERROR',
  NOT_FOUND = 'NOT_FOUND',
}

export { DeviceType, Status };
