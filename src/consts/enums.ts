enum DeviceType {
  CAR = 'CAR',
  MOBILE = 'MOBILE',
  COMPUTER = 'COMPUTER',
  WATCH = 'WATCH',
}

enum Status {
  SUCCESS = 'SUCCESS',
  ERROR = 'ERROR',
  NOT_FOUND = 'NOT_FOUND',
}

enum Environment {
  development = 'development',
  production = 'production',
}

export { DeviceType, Status, Environment };
