enum DeviceType {
  CAR = 'CAR',
  MOBILE = 'MOBILE',
  LAPTOP = 'LAPTOP',
  PC = 'PC',
}

enum Status {
  SUCCESS = 'SUCCESS',
  ERROR = 'ERROR',
  NOT_FOUND = 'NOT_FOUND',
}

enum Environment {
  DEV = 'development',
  PROD = 'production',
}

export { DeviceType, Status, Environment };
